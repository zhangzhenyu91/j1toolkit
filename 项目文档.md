# 检修一班工具箱 — 项目文档

> 班组数字化工具平台 · 微信小程序项目文档
> 版本：v1.2.2 ｜ 状态：本体（登录/权限/宫格）与首个应用「Call Me」已实现，待部署联调；UI 已定稿（方案五 × TDesign 定制）；其余技术方案为建议待评审

---

## 一、项目概述

「检修一班工具箱」是一个班组应用聚合平台微信小程序。小程序本体提供统一的登录与权限底座，各类班组应用（如 AI 知识库、智能助手、检修工单等）以子页面/入口的形式持续接入，为班组成员提供一站式的数字化工具入口。

### 1.1 项目定位

- 本项目首先作为**聚合各类应用的小程序本体**，主体能力为：登录（账号密码 + 微信登录两种方式）、应用权限控制（账号登录后仅显示拥有权限的应用入口）。
- 各应用作为小程序的子页面接入，整体界面风格应与小程序本体保持一致。

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| 统一登录 | 所有应用共用本体的账号密码登录与微信登录，不做各应用独立登录。 |
| 权限可控 | 账号登录后仅显示拥有权限的应用入口，权限按用户粒度管理。 |
| 易于扩展 | 应用会随项目推进不断增加，本体设计必须方便后续应用低成本接入。 |
| 数据集中 | 应用的数据存储与本体保持一致，即应用数据统一存储在本体的数据库中。 |

---

## 二、项目资源

| 资源 | 状态 | 说明 |
|------|------|------|
| 腾讯云服务器 | 已具备 | 以下服务端组件统一部署于该服务器。 |
| WeKnora 知识库 | 已部署完毕 | 访问地址 `https://know.j1net.com`，API 对接方式详见《WeKnora-API对接指南.md》。 |
| Dify | 已部署完毕 | AI 应用平台，配置从 env 读取。 |
| MySQL 数据库 | 已创建好空数据库 | 业务数据存储，配置从 env 读取。 |
| Redis | 已部署完毕 | 缓存/会话存储，配置从 env 读取。 |
| 腾讯云 COS | 已具备 | 对象存储（文件/图片等），配置从 env 读取。 |

**配置管理原则（既定要求）**：

- 所有资源的连接配置统一从环境变量（env）读取，不在代码中硬编码。
- 敏感信息（密钥、密码等）不提交入代码仓库。

---

## 三、功能需求

### 3.1 总体功能（项目全貌）

| # | 功能 | 说明 |
|---|------|------|
| 1 | 小程序本体：登录与权限 | 主体实现登录（账号密码、微信登录两种方式）与应用权限控制（账号登录后仅显示拥有权限的应用）。 |
| 2 | 应用作为子页面接入 | 各应用作为小程序的子页面，整体界面风格与小程序本体保持一致。 |
| 3 | 可持续扩展 | 应用随项目推进不断增加，本体设计方便后续扩展。 |
| 4 | 数据集中存储 | 应用的数据存储与本体保持一致，即应用数据存储在本体的数据库中。 |

### 3.2 本期范围（MVP）

| # | 功能 | 说明 |
|---|------|------|
| 1 | 小程序本体框架 | 本期仅开发小程序本体，包含整体框架与基础页面（登录页、首页·应用中心、我的）。 |
| 2 | 登录功能 | 账号密码登录 + 微信登录两种方式。 |
| 3 | 应用权限控制 | 账号登录后仅显示拥有权限的应用入口。 |
| 4 | 应用入口预留 | 首页预留应用入口位置（占位样式），本期不开发任何具体应用。 |

### 3.3 非目标（本期不做）

- 本期**不开发任何具体应用的业务功能**（如知识库问答、工单流转等业务逻辑均不在本期范围）。
- 各应用仅在本体中预留入口位置，后续按迭代逐个接入。

---

## 四、技术架构

> 前端、后端语言与部署方式**已定**；鉴权等细分方案仍为**建议，待评审**。

| 层 | 方案 | 状态 | 说明 |
|----|------|------|------|
| 前端 | 微信小程序原生 + tdesign-miniprogram | 已定 | UI 按定稿的方案五定制主题实现（见第八章）。 |
| 后端 | Node.js | 已定 | 与《WeKnora-API对接指南》示例同栈，便于对接 WeKnora、Dify 及 SSE 流式转发。 |
| 部署 | 云服务器 Docker | 已定 | 代码上传至云服务器，Node.js Docker 环境以 `npm run start` 启动；服务监听 `0.0.0.0`，端口从 env 读取。 |
| 鉴权 | JWT + Redis | 建议，待评审 | 登录签发 JWT，客户端以 `Authorization: Bearer <token>` 携带；Redis 存会话/黑名单，支持退出登录即时失效。 |
| 数据库 | MySQL | 资源已定 | 存业务数据（用户、应用、权限等），使用已创建的空库；表结构为建议，见第五章。 |
| 文件存储 | 腾讯云 COS | 资源已定 | 存文件类资源（头像、附件、图片等）。 |

---

## 五、数据库设计

> ⚠️ 本节为**建议方案，待评审**。本期仅建以下三张表，密码一律 bcrypt 哈希存储，不存明文。

### 5.1 sys_user — 用户表

| 字段 | 类型（建议） | 说明 |
|------|-------------|------|
| id | BIGINT UNSIGNED, PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(64), UNIQUE | 登录账号 |
| password_hash | VARCHAR(255) | 密码哈希，bcrypt 存储 |
| nickname | VARCHAR(64) | 昵称/姓名 |
| avatar | VARCHAR(512) | 头像地址（COS） |
| openid | VARCHAR(64), UNIQUE, 可空 | 微信 openid（微信登录时写入） |
| unionid | VARCHAR(64), 可空 | 微信 unionid |
| team | VARCHAR(64) | 所属班组，如「检修一班」 |
| role | VARCHAR(16)，DEFAULT 'user' | 角色：admin 管理员 / user 普通用户（老库启动时自动补列；`ADMIN_USERNAME` 指定账号始终为 admin） |
| status | TINYINT | 状态：1 正常，0 禁用 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.2 sys_app — 应用表

| 字段 | 类型（建议） | 说明 |
|------|-------------|------|
| id | BIGINT UNSIGNED, PK, AUTO_INCREMENT | 主键 |
| app_key | VARCHAR(64), UNIQUE | 应用唯一标识，如 `ai-kb` |
| name | VARCHAR(64) | 应用名称，如「AI知识库」 |
| icon | VARCHAR(512) | 图标（内置图标名或 COS 地址） |
| path | VARCHAR(255) | 小程序页面路径/分包路径 |
| sort | INT | 宫格排序，小的在前 |
| status | TINYINT | 状态：1 上架，0 下架；-1 占位（敬请期待） |

### 5.3 sys_user_app — 用户-应用权限关联表

| 字段 | 类型（建议） | 说明 |
|------|-------------|------|
| id | BIGINT UNSIGNED, PK, AUTO_INCREMENT | 主键 |
| user_id | BIGINT UNSIGNED | 用户 id，关联 sys_user.id |
| app_id | BIGINT UNSIGNED | 应用 id，关联 sys_app.id |
| created_at | DATETIME | 授权时间 |

- 约束：`user_id + app_id` 建**联合唯一索引**，防止重复授权。

### 5.4 后续扩展说明（建议）

- 当前权限模型为「用户 ↔ 应用」直接关联，简单直接，满足本期需求。
- 后续用户量/权限管理复杂后，可平滑扩展 `sys_role`（角色表）与 `sys_user_role` / `sys_role_app`，由「按人授权」升级为「按角色授权」，不影响现有三张表结构。

---

## 六、接口设计

> ⚠️ 本节为**建议方案，待评审**。统一约定：除登录类接口外，所有接口通过请求头 `Authorization: Bearer <JWT>` 鉴权；响应统一包 `{ code, message, data }` 结构（示例见下）。
>
> **自动登录（已实现）**：小程序以首页为入口，启动时先校验本地 token（调 `/user/profile`）；token 失效且设备已绑定微信（`wx_bound`）时，自动以 `wx.login` code 调 `/auth/wx-login` 静默换发新 token，全程无需用户操作；未绑定微信的用户才会落到登录页。

### 6.1 接口清单

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/auth/login` | 否 | 账号密码登录，返回 JWT；可选携带 `wx_code`：账号未绑定微信时自动绑定当前微信号（若该 openid 已关联历史独立账号则转移至本账号），响应含 `wx_bound` 标识 |
| POST | `/auth/wx-login` | 否 | 微信登录：小程序 wx.login 得 code，后端用 code 向微信换 openid，匹配/创建用户后返回 JWT |
| POST | `/auth/logout` | 是 | 退出登录，服务端使当前 token 失效（Redis 黑名单/删除会话） |
| GET | `/user/profile` | 是 | 获取当前用户信息（昵称、头像、班组、角色、`wx_bound` 微信绑定状态等） |
| GET | `/app/list` | 是 | 获取**当前用户可见**的应用列表（按权限过滤 + 按 sort 排序） |
| POST | `/callme/sessions` | 是 + call-me 权限 | 「Call Me」新建 WeKnora 会话 |
| GET | `/callme/sessions` | 是 + call-me 权限 | 「Call Me」会话列表（透传 WeKnora） |
| GET | `/callme/sessions/{id}` | 是 + call-me 权限 | 「Call Me」会话详情/历史消息（透传 WeKnora） |
| PUT | `/callme/sessions/{id}` | 是 + call-me 权限 | 「Call Me」修改会话名称（透传 WeKnora `PUT /sessions/{id}`） |
| DELETE | `/callme/sessions/{id}` | 是 + call-me 权限 | 「Call Me」删除会话 |
| DELETE | `/callme/sessions/{id}/messages/{msgId}` | 是 + call-me 权限 | 「Call Me」删除单条消息（透传 WeKnora `DELETE /messages/{session_id}/{id}`） |
| POST | `/callme/chat` | 是 + call-me 权限 | 「Call Me」发送消息，SSE 流式返回；服务端将 WeKnora 事件归一化为 `thinking` / `answer` / `done` / `error` 转发 |

> 「Call Me」接口已实现（`server/src/routes/callme.js`）：需在 env 配置 `WEKNORA_API_URL` / `WEKNORA_API_KEY` / `WEKNORA_AGENT_ID`，SSE 兼容 WeKnora `data:` 无空格格式，详见《WeKnora-API对接指南》。所有 WeKnora 调用携带 `X-External-User-ID` 请求头（取**登录账号**本身，如 `zhangzhenyu91`，不带前缀；agent-chat 另带 `channel: "api"`），**会话按用户隔离**——各用户只能看到/取回自己的会话与历史。

### 6.2 管理接口（需 admin 角色）

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/admin/users?keyword=` | 是 + admin | 员工列表（员工管理） |
| POST | `/admin/users` | 是 + admin | 新建员工账号（密码 bcrypt） |
| PUT | `/admin/users/{id}` | 是 + admin | 修改员工昵称/班组/状态/角色，或重置密码；不能禁用或降级自己的账号 |
| GET | `/admin/apps` | 是 + admin | 全部应用列表 |
| GET | `/admin/users/{id}/apps` | 是 + admin | 某员工已授权的应用 id 列表 |
| PUT | `/admin/users/{id}/apps` | 是 + admin | 设置员工应用授权（全量替换，事务；自动过滤不存在的 app_id） |

> 管理接口已实现（`server/src/routes/admin.js`）；小程序端对应「我的 → 管理功能」下的员工管理、权限管理页（仅 `role=admin` 可见）。

### 6.3 示例：POST /auth/login

请求：

```json
{
  "username": "zhangsan",
  "password": "明文密码仅用于传输，服务端比对 bcrypt 哈希"
}
```

响应：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "expires_in": 604800,
    "user": {
      "id": 1,
      "username": "zhangsan",
      "nickname": "张三",
      "avatar": "",
      "team": "检修一班"
    }
  }
}
```

### 6.4 示例：GET /app/list

请求头：`Authorization: Bearer eyJhbGciOi...`

响应（仅返回当前用户有权限的应用；「敬请期待」占位由前端按固定样式渲染，或由后端以 status=-1 下发，定稿时二选一）：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "list": [
      {
        "app_key": "ai-kb",
        "name": "AI知识库",
        "icon": "ai-kb",
        "path": "/pkg-aikb/pages/index/index",
        "sort": 1
      },
      {
        "app_key": "assistant",
        "name": "智能助手",
        "icon": "assistant",
        "path": "/pkg-assistant/pages/index/index",
        "sort": 2
      }
    ]
  }
}
```

---

## 七、扩展性设计

> ⚠️ 本节为**建议方案，待评审**。核心目标：新应用接入不改本体框架代码。**落地执行手册见《开发指南.md》**。

- **新增一个应用** = 两步：① 向 `sys_app` 插入一条应用配置（app_key / 名称 / 图标 / 页面路径 / 排序）；② 在小程序中以**分包**形式新增该应用页面。首页宫格读取 `/app/list` 自动出现新入口，无需改首页代码。
- **给用户开通应用权限** = 向 `sys_user_app` 插入一条记录（user_id + app_id），即时生效。
- **应用页面 UI** 必须遵循本体设计规范（配色、字号、间距、组件风格与本体一致），保证整体风格统一；设计规范在 UI 评审定稿后沉淀。
- **应用数据** 统一存储于本体 MySQL 库（各应用建各自业务表），文件统一存 COS。

### 7.1 已接入实例：「Call Me」

首个应用「Call Me」（app_key：`call-me`，WeKnora AI 知识库问答）已按上述模式接入：小程序分包 `pkg-callme/`（会话列表 + SSE 流式对话页）+ `sys_app` 应用记录（首次启动自动写入）。对话内容存储于 WeKnora 侧，本体不另存聊天记录（后续如有审计需求可再评估落库）。进入行为：首页点击 Call Me 先到会话列表，再自动进入最近一次对话（无对话则自动创建）；从对话页返回列表时不触发自动进入。

---

## 八、UI 设计

**已定稿：方案五「安全橙 · 浅色工业风」，基于 tdesign-miniprogram 组件库定制主题实现。**

- 设计稿仅保留定稿方案五：`design/style-5.html`（其余 4 套候选稿已移除，可在 git 历史中找回）；定稿设计 token 以 8.1 表为准。
- 实现方式：组件使用 tdesign-miniprogram，在全局样式（`app.wxss`）覆盖 `--td-*` CSS 变量注入定稿 token；警示斜纹、描边徽章等特征元素用 wxss 自绘。
- 覆盖页面：登录页、首页·应用中心、我的；后续应用子页面必须沿用同一套 token，与本体风格一致。

### 8.1 定稿设计 token

| 用途 | 值 | 说明 |
|------|----|------|
| 品牌主色 / 激活态 | `#F26D21` | 安全橙：激活 Tab、链接、勾选框、宫格图标、按钮点缀 |
| 主文字 / 藏青主色 | `#22314E` | 导航标题、登录页横幅、主按钮底色 |
| 页面背景 | `#F4F1EA` | 暖纸底 |
| 卡片 / 容器 | `#FFFFFF`，圆角 10px | 用户卡片顶部 8px 橙青警示斜纹条（特征元素） |
| 输入框 | 白底、1px `#E3DCCB` 描边、圆角 8px | |
| 发丝分隔线 | `#E8E0CD` / `#E3DCCB` | |
| 次级文字 | `#6B7690` | |
| 占位文字 | `#A89F8A` | |
| 图标浅底 | `#FDEEE2` | 配橙色线性图标 |
| 危险操作 | `#CF4444` | 退出登录等 |
| 虚线占位 | `#D9CFBA` | 「敬请期待」应用入口预留 |
| 板块标题 | 文字左侧 4px 橙色竖条 | |
| 身份标签 | 橙色描边徽章 | 如「高级技师」 |

> 实现细则同步维护在 `AGENTS.md` 第四节；修改设计规范时两处须同步更新。

---

## 九、环境变量清单建议

> ⚠️ 本节为**建议清单，待评审**。所有配置统一从 env 读取，敏感信息不入仓。

| 变量名 | 说明 |
|--------|------|
| `WX_APPID` | 微信小程序 AppID |
| `WX_SECRET` | 微信小程序 AppSecret（code 换 openid 用） |
| `JWT_SECRET` | JWT 签名密钥 |
| `JWT_EXPIRES` | JWT 有效期（如 `7d`） |
| `MYSQL_HOST` | MySQL 主机 |
| `MYSQL_PORT` | MySQL 端口 |
| `MYSQL_USER` | MySQL 用户名 |
| `MYSQL_PASSWORD` | MySQL 密码 |
| `MYSQL_DATABASE` | MySQL 数据库名（已建空库） |
| `REDIS_HOST` | Redis 主机 |
| `REDIS_PORT` | Redis 端口 |
| `REDIS_PASSWORD` | Redis 密码 |
| `COS_SECRET_ID` | 腾讯云 COS SecretId |
| `COS_SECRET_KEY` | 腾讯云 COS SecretKey |
| `COS_BUCKET` | COS 存储桶 |
| `COS_REGION` | COS 地域 |
| `WEKNORA_API_URL` | WeKnora API 地址（`https://know.j1net.com/api/v1`） |
| `WEKNORA_API_KEY` | WeKnora API Key（`X-API-Key` 请求头，详见《WeKnora-API对接指南》） |
| `DIFY_API_URL` | Dify API 地址 |
| `DIFY_API_KEY` | Dify API Key |
