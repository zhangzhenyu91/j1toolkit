# 开发指南 —— 检修一班工具箱

> 开发全参考：架构、数据库、接口、已接入应用对接细节、添加新应用手册、踩坑约定。
> 协作规则与 UI 定稿 token 以 `AGENTS.md` 为准；部署与环境变量以 `README.md` 为准。

## 一、现状速览

```
微信小程序 ──HTTPS──▶ https://j1net.com/j1toolkit/（1Panel 反代，剥前缀兼容）
                          │
                          ▼
                  server/（Express，npm run start，0.0.0.0:PORT）
                    ├─ MySQL：sys_user / sys_app / sys_user_app / worklog_*
                    ├─ Redis：JWT 黑名单
                    ├─ WeKnora：Call Me 对话代理（SSE）
                    ├─ COS：出工日志水印照片（WORKLOG_ENABLED 开关）
                    └─ Dify：出工日志照片验证工作流
```

已实现：登录（账号密码 + 微信 + 自动登录续期）、首页权限宫格、「我的」（含管理员功能区）、员工管理、权限管理、Call Me（AI 知识库对话，分包 `pkg-callme`）、出工日志（分包 `pkg-worklog`，后端 `server/src/worklog/`，`WORKLOG_ENABLED` 开关）、安全日活动记录（网页端 SafeDayLogs，登录经主平台 app-login 校验应用权限；小程序为引导页分包 `pkg-safeday`）。

关键目录：

| 目录 | 内容 |
|------|------|
| `miniprogram/` | 主包（登录/首页/管理页 + 公共组件 navbar、tabbar + utils；「我的」与首页为同页双面板，tabbar 切换走 swiper 左右滑动，无独立 pages/me） |
| `miniprogram/pkg-callme/` | Call Me 分包（新应用的参照模板） |
| `miniprogram/pkg-worklog/` | 出工日志分包（主页[全部操作一级页面化] / 常用数据管理页 + style/common.wxss） |
| `server/src/` | 后端：routes / middleware / services / db.js（建表+种子） |
| `server/src/worklog/` | 出工日志后端子模块（schema 建表种子 / cos / dify / verify / index 路由） |
| `SafeDayLogs/` | 安全日活动记录生成平台（独立部署的网页端应用，登录验证走主平台，见第八节） |

## 二、架构与鉴权

- **统一响应**：`{ code, message, data }`，code=0 成功；除登录类接口外均经 `Authorization: Bearer <JWT>` 鉴权。
- **JWT + Redis**：登录签发 JWT；Redis 存黑名单（退出登录即时失效）。
- **自动登录（已实现）**：小程序以首页为入口，启动先校验本地 token（调 `/user/profile`）；token 失效且设备已绑定微信（`wx_bound`）时，自动以 `wx.login` code 调 `/auth/wx-login` 静默换发新 token，全程无需用户操作；未绑定微信才落到登录页。新页面不要绕过 `app.js` 的 bootstrap 门控，参照首页 `gate` 模式。
- **账号密码登录自动绑微信**：`/auth/login` 可携带 `wx_code`，账号未绑定微信时自动绑定当前微信号（若该 openid 已关联历史独立账号则转移至本账号）。
- **反代前缀**：`PROXY_PREFIX` 已兼容剥/不剥前缀两种转发。
- **角色**：`sys_user.role`（admin/user），`ADMIN_USERNAME` 指定账号始终为 admin（防误改）；管理接口挂 `requireAdmin`（实时查库）。
- **应用权限**：路由统一 `router.use(auth, requireApp('<app-key>'))`（登录 + 应用权限双校验）。

## 三、数据库设计

> 密码一律 bcrypt 哈希存储。首次启动自动建表/补列/写种子（`server/src/db.js`、`server/src/worklog/schema.js`）。

### 3.1 sys_user — 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED, PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(64), UNIQUE | 登录账号 |
| password_hash | VARCHAR(255)，可空 | 密码 bcrypt 哈希（微信创建的用户可为空） |
| nickname | VARCHAR(64) | 昵称/姓名 |
| avatar | VARCHAR(512) | 头像地址 |
| openid | VARCHAR(64), UNIQUE, 可空 | 微信 openid |
| unionid | VARCHAR(64), 可空 | 微信 unionid |
| team | VARCHAR(64) | 所属班组 |
| role | VARCHAR(16)，DEFAULT 'user' | admin / user |
| status | TINYINT | 1 正常 0 禁用 |
| created_at / updated_at | DATETIME | |

### 3.2 sys_app — 应用表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED, PK | 主键 |
| app_key | VARCHAR(64), UNIQUE | 应用唯一标识（如 `call-me` / `work-log`） |
| name | VARCHAR(64) | 应用名称 |
| icon | VARCHAR(64) | TDesign 图标名 |
| path | VARCHAR(255) | 小程序页面路径 |
| sort | INT | 宫格排序，小的在前 |
| status | TINYINT | 1 上架 0 下架（-1 占位「敬请期待」，由前端按固定样式渲染） |
| created_at | DATETIME | |

### 3.3 sys_user_app — 用户-应用权限关联表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED, PK | 主键 |
| user_id / app_id | BIGINT UNSIGNED | 关联用户/应用，`user_id + app_id` 联合唯一索引 |
| created_at | DATETIME | 授权时间 |

后续权限复杂后可平滑扩展 `sys_role` / `sys_user_role` / `sys_role_app`，不影响现有三表。

### 3.4 出工日志业务表（`WORKLOG_ENABLED=true` 时自动建）

一条日志卡片 = 一次派车；「未出车」即 vehicle_id 为 NULL。

**worklog_entry — 日志主表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED, PK | 主键 |
| log_date | DATE，索引 | 日志日期 |
| patrol_content | TEXT，可空 | 巡视内容 |
| vehicle_id | BIGINT UNSIGNED，可空 | 车牌；NULL=未出车 |
| destination_id | BIGINT UNSIGNED，可空 | 目的地 |
| created_by | BIGINT UNSIGNED | 创建人 |
| created_at / updated_at | DATETIME | |

**worklog_entry_member — 用车人/打卡表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED, PK | 主键（打卡接口的 mid 即此 id） |
| entry_id | BIGINT UNSIGNED | 关联主表；`entry_id + member_id` 联合唯一 |
| member_id | BIGINT UNSIGNED | 关联 worklog_member.id（成员由管理端维护，不支持自定义人名） |
| checked | TINYINT，DEFAULT 0 | 0 未打卡（红叉）/ 1 已打卡（绿勾） |
| sort | INT | 展示顺序（与成员 sort 一致） |

**worklog_photo — 水印照片表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED, PK | 主键 |
| entry_id | BIGINT UNSIGNED，索引 | 关联主表 |
| cos_key | VARCHAR(255) | COS 对象键（`COS_WORKLOG_PREFIX` 前缀） |
| url | VARCHAR(512) | 访问地址 |
| members | JSON | 所属人名数组（可多人；每人全卡片最多一张） |
| verify_status | VARCHAR(16)，DEFAULT 'pending' | pending 验证中 / passed 通过 / date_mismatch 日期不符 / dest_mismatch 目的地不符 / failed 验证失败 |
| work_content | VARCHAR(512) | Dify 返回施工内容（title） |
| lng / lat | VARCHAR(32) | Dify 返回经纬度 |
| created_at | DATETIME | 上传时间（超时兜底判定依据） |

**worklog_vehicle / worklog_destination / worklog_member — 三类字典**（结构同构）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED, PK | 主键 |
| plate_no / name / name | VARCHAR, UNIQUE | 车牌号 / 目的地名称 / 成员姓名 |
| sort | INT | 排序（成员 sort 即点亮按钮顺序） |
| status | TINYINT，DEFAULT 1 | 1 启用 0 停用（停用不再出现在候选，历史日志照常显示） |
| created_at | DATETIME | |

成员种子（首次启动、空表时写入，sort 1-7）：赵登、郑海楠、任舒诺、薛忠亮、曹万鑫、张振宇、高麒涵。

## 四、接口清单

> 统一 `{ code, message, data }`；除登录类外均需 `Authorization: Bearer <JWT>`。

### 4.1 本体接口

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/auth/login` | 否 | 账号密码登录，返回 JWT；可携带 `wx_code` 自动绑定微信，响应含 `wx_bound` |
| POST | `/auth/wx-login` | 否 | 微信登录：code 换 openid，仅匹配已绑定账号的用户返回 JWT；未绑定返回 40313，提示先账号密码登录一次完成绑定 |
| POST | `/auth/app-login` | 否 | 应用登录校验（供 SafeDayLogs 等外部网页应用调用）：账号密码 + 指定 `app_key` 应用权限一次校验，成功仅返回用户信息，不签发 JWT（会话由调用方自管） |
| POST | `/auth/logout` | 是 | 退出登录，token 失效 |
| GET | `/user/profile` | 是 | 当前用户信息（含角色、`wx_bound`） |
| GET | `/app/list` | 是 | 当前用户可见应用列表（按权限过滤 + sort 排序） |

### 4.2 Call Me（需 call-me 权限，实现见 `server/src/routes/callme.js`）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/callme/sessions` | 新建会话 |
| GET | `/callme/sessions` | 会话列表（透传 WeKnora） |
| GET | `/callme/sessions/{id}` | 会话详情/历史消息 |
| PUT | `/callme/sessions/{id}` | 修改会话名称 |
| DELETE | `/callme/sessions/{id}` | 删除会话 |
| DELETE | `/callme/sessions/{id}/messages/{msgId}` | 删除消息（问答成对删除：按 request_id 连同配对问题/回答） |
| POST | `/callme/chat` | 发送消息，SSE 流式返回；WeKnora 事件归一化为 `thinking` / `answer` / `done` / `error` 转发 |

### 4.3 出工日志（需 work-log 权限，`WORKLOG_ENABLED=true` 时挂载于 `/api/v1/worklog`）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/worklog/meta` | 车牌/目的地/成员列表（前端自行补「未出车」固定项） |
| GET | `/worklog/logs?date=&scope=` | 某日卡片全量（含用车人、打卡、照片、verify_passed 与未通过明细 verify_reasons）；`scope=mine` 仅用车人含当前用户（按昵称匹配成员）的卡片 |
| GET | `/worklog/day-status?month=&scope=` | 当月每日验证状态映射，供日历着色；`scope=mine` 个人口径（见 7.2） |
| POST | `/worklog/logs` | 新建卡片（log_date / patrol_content 可空 / vehicle_id 可空=未出车 / destination_id / member_ids[]） |
| PUT | `/worklog/logs/{id}` | 修改卡片（用车人全量替换，保留仍在名单者的打卡状态） |
| DELETE | `/worklog/logs/{id}` | 删除卡片（先删 COS 对象再删行） |
| PUT | `/worklog/logs/{id}/members/{mid}/check` | 打卡切换（mid = worklog_entry_member.id） |
| GET | `/worklog/photos?from=&to=` | 日期范围内全部照片（批量下载），按 log_date + 上传序排列 |
| GET | `/worklog/report?from=&to=&scope=` | 范围内验证不通过记录及全部原因（查看报告），按 log_date + 卡片序排列；`scope=mine` 仅「我未打卡 / 我未上传水印照片 / 我的水印照片未通过」的卡片且原因只列个人相关项（见 7.1） |
| GET | `/worklog/geo?lng=&lat=` | 按经纬度取当前「地点 + 天气」（和风），加水印弹层无历史照片时预填用（见 7.3） |
| POST | `/worklog/logs/{id}/photos` | 照片上传（base64 → COS），异步触发 Dify 验证；body 带 `wm` 对象时服务端先渲染水印再传 COS（见 7.3） |
| PUT | `/worklog/photos/{id}/members` | 修改照片所属人名 |
| POST | `/worklog/photos/{id}/verify` | 重新验证（仅 failed 可用，重置 pending 异步重调 Dify） |
| DELETE | `/worklog/photos/{id}` | 删除照片（同步删 COS 对象） |

后端兜底校验：未出车拒绝目的地/成员/照片；有照片的卡改未出车需先删照片；被移出用车人若已有照片则拒绝；每人限一张水印照片（按 members 数组查重）。

### 4.4 管理接口（需 admin 角色；worklog 管理接口再叠 work-log 权限）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/admin/users?keyword=` | 员工列表 |
| POST | `/admin/users` | 新建员工（密码 bcrypt） |
| PUT | `/admin/users/{id}` | 改昵称/班组/状态/角色或重置密码；不能禁用或降级自己 |
| GET | `/admin/apps` | 全部应用列表 |
| GET | `/admin/users/{id}/apps` | 某员工已授权应用 id 列表 |
| PUT | `/admin/users/{id}/apps` | 设置员工应用授权（全量替换，事务） |
| GET/POST/PUT/DELETE | `/worklog/admin/vehicles` | 车牌号维护 |
| GET/POST/PUT/DELETE | `/worklog/admin/destinations` | 目的地维护 |
| GET/POST/PUT/DELETE | `/worklog/admin/members` | 出工成员维护 |

字典类管理接口同构：新增（sort 自动追加）/改名/排序/停启用；DELETE 仅允许无引用记录，否则提示改为停用。

## 五、添加新应用（五步 checklist）

### 1. 数据库登记应用（必须）

```sql
INSERT INTO sys_app (app_key, name, icon, path, sort, status)
VALUES ('my-app', '应用名称', 'robot', '/pkg-myapp/pages/index/index', 3, 1);
```

- `icon` 填 TDesign 图标名（不带 `t-icon-` 前缀）；`path` 填分包页面路径；
- 也可仿照 Call Me / 出工日志，在 `db.js` 或应用 schema 模块加种子，启动时自动写入。
- 给用户授权：**管理员在「我的 → 权限管理」勾选即可**，无需手写 SQL。

### 2. 小程序分包（必须）

1. 参照 `pkg-callme/` 新建 `pkg-myapp/pages/...`；
2. `app.json` 的 `subPackages` 注册；
3. 首页宫格由 `/app/list` 驱动，授权后自动出现入口，**无需改首页代码**；
4. 分包若有自己的 npm 依赖：在 `pkg-myapp/` 下新建 `package.json` 并 `npm install`（主包用 → `miniprogram/` 装；分包专用 → 分包目录装）；`project.config.json` 的 `packNpmRelationList` 加一行；重新「构建 npm」。

### 3. 后端接口（按需）

1. 新建 `server/src/routes/myapp.js`（或仿出工日志建 `server/src/myapp/` 子模块 + env 开关），路由统一 `router.use(auth, requireApp('my-app'))`；
2. `server/src/index.js` 挂载；
3. 对接外部服务：配置走 env——`config.js` 增加读取项、`.env.example` 增加示例、service 里加 `ensureConfigured()` 守卫（参照 `services/weknora.js`）；
4. SSE 流式场景参照 `routes/callme.js`：`X-Accel-Buffering: no` 响应头、15s 心跳、`utils/sse.js` 解析器、客户端断开销毁上游。

### 4. 自检与交付

`node --check` 全部 JS、JSON 合法性校验；本地无法模拟的项在交付说明中如实标注「待云端/真机验证」。

### 5. 页面分享（必须）

所有页面都要实现 `onShareAppMessage`，统一走 `utils/share.js` 的 `shareAppMessage(this, { app, title, path })`：

- `app` 传 `sys_app.app_key`（分享图用应用图标）；非应用页面（首页/登录/管理等）不传，用工具箱分享图；
- 标题自动拼「xxx · 检修一班工具箱」；分享路径默认当前页，登录页固定落 `/pages/home/home`（首页门控自动处理未登录跳转）；
- 分享图由 `design/make_share_images.py` 生成：背景统一（暖纸底 + 顶部橙青警示斜纹条），中央白色圆角卡片 + 图标，**尺寸固定 5:4（1000×800，微信分享缩略图按 5:4 裁剪显示，源头必须就是 5:4，否则图标会被裁偏；单张压到 100KB 以内，体验评分「图片和音频资源不超过 200K」才不报错）**。新应用接入时：① 查图标码点——`miniprogram/node_modules/tdesign-miniprogram/miniprogram_dist/icon/icon.wxss` 中 `.t-icon-<图标名>:before` 的 `content`；② 在脚本 `APPS` 字典加 `'app-key': 0x码点` 后重跑，生成 `miniprogram/images/share/share-<app-key>.jpg`；③ `utils/share.js` 的 `SHARE_IMAGES` 加一行映射；
- 图标字形渲染自 TDesign 图标 webfont `design/font/t.ttf`（版本见上述 icon.wxss 的 `@font-face` URL），升级 tdesign-miniprogram 大版本后需重新下载同步。

## 六、已接入应用：Call Me（WeKnora 对接）

app_key `call-me`；对话内容存 WeKnora 侧，本体不另存聊天记录。进入行为：首页点击先到会话列表，再自动进入最近一次对话（无对话则自动创建）；从对话页返回列表时不触发自动进入。

WeKnora 对接要点（原《WeKnora-API对接指南》已并入本节）：

- **认证**：所有请求带 `X-API-Key` 请求头（**不是** Authorization Bearer）；另带 `X-External-User-ID`（取本系统**登录账号**本身，如 `zhangzhenyu91`，不带前缀）——不传会导致会话列表/历史取不回且各调用方会话互相可见；`agent-chat` 另带 `channel: "api"`。会话按用户隔离。
- **会话接口**：`POST /sessions` 创建（`data.id` 即 session_id）；`GET /sessions` 列表；`GET /sessions/{id}` 仅元数据；`GET /messages/{id}/load` 历史消息（before_time/limit 分页）；`PUT/DELETE /sessions/{id}`；`DELETE /messages/{session_id}/{id}` 删单条消息；`POST /agent-chat/{session_id}` 发送（payload：query、agent_id、agent_enabled、enable_memory、images `[{data: dataURL}]`）。
- **SSE 解析（最大的坑）**：WeKnora 返回 `data:{json}`——**data: 后无空格**，解析必须兼容两种写法；事件以 `\n\n` 分隔；`response_type` 有 agent_query / thinking / tool_call / tool_result / references / answer / error，取 `answer` 增量拼接，`done=true` 结束。
- **服务端转发给小程序**：响应头 `Content-Type: text/event-stream` + `Cache-Control: no-cache, no-transform` + `X-Accel-Buffering: no`（禁 Nginx 缓冲）+ `flushHeaders()`；归一化事件后 `res.write("data: {json}\n\n")`；15s 心跳；客户端断开要销毁上游流。
- **小程序端**：`wx.request` 必须 `enableChunked: true` + `responseType: 'text'`，用 `requestTask.onChunkReceived` 收流（ ArrayBuffer 需转字符串，注意 UTF-8 边界）；不支持时以 success 回调兜底。
- **图片**：直接 base64 dataURL 随 `images` 上送，不需先传 COS；因此 `express.json` 限制已放宽到 12mb，反代需 `client_max_body_size 20m`。
- **反代**：SSE 必须 `proxy_buffering off` + `proxy_read_timeout 300s`。

## 七、已接入应用：出工日志

需求与设计：`design/worklog.html`（定稿，9 屏）。app_key `work-log`；env `WORKLOG_ENABLED=true` 时启动自动建表、写 sys_app 记录与 7 名成员种子、挂载路由。

### 7.1 交互与业务规则

- 进入默认当天，日期条 ±1 天、页面左右滑动切换日期（横向位移 ≥60px 且 |dx|>2|dy| 才触发；两段式平移动效：当前内容沿滑动方向移出、新内容从对侧滑入）、t-calendar 弹层选任意日期；一天可多卡片，一卡片 = 一次派车。
- **一级页面化（无二级编辑页）**：新建与「改派车/用车人」共用主页底部表单弹层（内联筛选下拉；无实时保存，点「保 存」提交，遮罩关闭 = 放弃修改）；改派车保存前弹警告「请确保内网派车单同步修改！」，点「我确认已修改」后才 PUT；新建不含巡视内容；巡视内容点卡片主块单独弹层修改（快捷输入同水印施工内容，保存才提交；PUT 为全量替换，车牌/目的地/用车人沿用卡片原值）；卡片上直接打卡切换（红叉↔绿勾）、照片增删改人名保持即时生效；操作区仅「删除」。
- 新建默认「未出车」，选具体车牌后展示人员段；「未出车」时目的地置灰、人员/打卡/照片隐藏，角标「免验证」不参与记录验证。
- 车牌/目的地/人员三类字典由 admin 在分包内管理页维护（添加/改名/停启用，删除仅限无引用）；不支持自定义人名。
- **卡片完整展示**：车牌号下方为「巡视内容」主内容块（橙条浅底、字号最大，与其他信息区分），其下依次为打卡情况（含全部用车人，不再单设用车人员行）、水印照片；右上角角标 = 记录验证状态（verify_passed，实时计算不落库）：
  1. 未出车不验证（exempt）；
  2. 目的地已选且有用车人（巡视内容按需求可空，不计入）；
  3. 至少一张水印照片且全部已通过；
  4. 用车人名单与全部照片人名并集一致；
  5. 多张照片施工内容一致；
  6. 全部用车人已打卡。
  任一不满足即 failed（红「未通过」），全满足 passed（绿「验证通过」）；failed 时车牌头部下方红底块逐行列出全部不满足项（`verify_reasons`，与状态同源同口径——后端 `computeFailReasons` 计算：未选目的地 / 未选用车人 / 谁未打卡 / 未上传照片 / 逐张照片未通过项（人名＋验证中·验证失败·日期不符·地点不符）/ 谁未上传照片 / 照片人名不在名单 / 施工内容不一致）。
- **视图管理**：主页「全部 / 仅看我」开关（`scope=mine`，按 sys_user.nickname 匹配 worklog_member.name，不同名则个人视图为空）；个人口径日历着色：仅含我的日期着色，我未打卡 / 没有含我名字的水印照片 / 含我照片未通过（含验证中/失败）→ 红，否则绿；全局面径：全部通过=绿，有未通过=红，无记录或仅免验证=不着色。
- **日历着色实现**：t-calendar `format` 属性注入单元格 className（selectComponent setData 注入函数，无单元格插槽），数据取 `/worklog/day-status`；「今天」加粗 + 橙色小圆点。
- **批量下载**：日期范围默认「当天 1~10 日 → 上月整月；11 日及以后 → 本月 1 号至当天」，t-calendar type="range" 可改（与下载面板互斥开合规避叠层 z-index）；照片按月分卡、3 列网格、下标几日、点图 wx.previewImage、角圈批量勾选；「仅看我」开关前端筛选仅含自己名字的照片（按昵称匹配成员名，同 scope=mine 口径）；下载按 (log_date, id) 排序串行 `wx.downloadFile` + `wx.saveImageToPhotosAlbum`（顺序从 1 号开始）；相册授权走 `scope.writePhotosAlbum`；**COS 域名需配置 downloadFile 合法域名**。
- **查看报告**：底部「查看报告」面板（复用批量下载面板骨架与默认日期范围；两面板共用同一 range 日历实例，`_rangeCalFor` 标记回开对象）；取 `/worklog/report` 按日期分组列出范围内 verify_passed=failed 的卡片（车牌、用车人、全部不通过原因）；面板内「仅看我」开关走后端 `scope=mine`（非前端筛选）：仅含「我未打卡 / 我未上传水印照片（用车人含我但无照片含我名字）/ 我的水印照片未通过（含验证中/失败）」的卡片，且原因只列个人相关项（「我未打卡」「我未上传水印照片」「我的水印照片××」）。

### 7.2 照片与 Dify 验证（原《打卡日志-dify-api.md》已并入本节）

链路：小程序 `wx.chooseMedia` → base64 上送（沿用 Call Me 先例）→ 后端转存 COS（key：`{COS_WORKLOG_PREFIX}{yyyy.MM.dd}/{entryId}-{ts}.jpg`）→ 插行 pending → **异步**调 Dify → 回写；前端 3s 轮询 pending。

Dify 调用（`server/src/worklog/dify.js`）：

- `POST {DIFY_API_URL}/v1/workflows/run`（`/v1` 由代码拼接，env 只填域名；已带 /v1 不重复）；`Authorization: Bearer {DIFY_WORKLOG_API_KEY}`；
- body：`{ inputs: { date: 'YYYY.MM.DD', destination: '目的地名称', picture: { transfer_method: 'remote_url', url: 'COS图片地址', type: 'image' } }, response_mode: 'blocking', user: '登录账号' }`；
- **坑**：`picture` 是单文件变量必须传**对象**，传数组 `[{...}]` 会报 `invalid_param: picture in input form must be a file`；
- 响应 `data.data.outputs`：`date_verify` / `destination_verify`（'true'/'false'）、`title`（施工内容）、`time`（拍摄时间）、`weather`（天气）、`location`（地点）、`lng` / `lat`（经纬度）；
- 映射：任一 verify 为 'false' → `mismatch`（`date_ok`/`dest_ok` 两列逐项记录，前端据以枚举未通过项，如「日期不符、地点不符」），均 true → `passed`，调用异常 → `failed`；新输出落列为 `shot_time` / `weather` / `location`（老库启动自动补列）；
- 排障：4xx 时日志会打出 Dify 响应体与入参（变量名不符、工作流未发布、Dify 无法访问 COS 地址等按此定位）。

兜底与重试：

- **重新验证**：`POST /photos/{id}/verify`（仅 failed 可用）重置 pending 并异步重调；卡片上 failed 状态旁显示「重新验证」按钮；
- **超时兜底**：读取日志时 `pending` 超 10 分钟自动置 failed（防服务端重启丢回写后永远停在验证中）；
- 小程序在上传完成后即可关闭，验证全程在服务端闭环，不受影响。

照片展示：右侧与缩略图等高（不溢出），自上而下八项——① 照片验证情况（核验通过绿标 / 逐项列出未通过项红标，如「日期不符、地点不符」；验证失败 failed 时旁置「重新验证」）② 照片所属人员（人名标签，点击弹点亮框修改，候选=本卡用车人，已有照片者置灰「已上传」）③ 施工内容（直接显示值，行尾复制按钮点击复制）④ 拍摄时间（time）⑤ 天气（weather）⑥ 地点（location）⑦ 经度（lng）⑧ 纬度（lat）；标签与内容字号统一。

### 7.3 「选择照片并添加水印」（服务端加水印）

「添加水印照片」点击后弹自绘二选一弹层（不用 `t-action-sheet`：其仅分包使用，tdesign 整体构建进主包会触发「主包未使用 JS」审计警告）：① **选择水印照片上传**＝7.2 原链路不变；② **拍摄/选择照片并添加水印**＝人名点亮（确认区多出「拍摄」按钮：「选择照片」＝相册原图 /「拍摄」＝相机原图）→ 取原图 → **4:3 裁剪层**（拍摄必裁；相册经 `wx.getImageInfo` 判定已是 4:3/3:4（±0.02 容差）则免裁直进表单，EXIF 旋转 90/270° 时宽高互换判定；横拍锁横向 4:3、纵拍锁纵向 3:4）→ 底部弹层编辑水印字段 → 原图 base64 + `wm` 字段一起上送 → **服务端渲染水印**（天气留空则不渲染「天气」行，卡片高度随行数自适应）→ 与①同一 COS/插库/Dify 验证链路。

- **裁剪层实现**：`movable-area`（取景框，尺寸按方向锁定 4:3/3:4）+ `movable-view`（拖拽 + 双指缩放，缩放原点为视图中心，图片 cover 适配并居中），确认时可视区换算到图片像素、离屏 `type=2d` canvas 重绘导出（长边 ≤2560，JPEG quality 0.92；`createImage` 解码应用 EXIF，宽高未按 EXIF 互换的个别机型按 orientation 手动旋转兜底；导出期间取消则丢弃结果）。

- **水印渲染**：`server/src/worklog/watermark.js`（自「今日水印相机·工程记录」样式 1:1 还原的 Canvas 2D 渲染核心，无 DOM 依赖）+ `server/src/worklog/render-photo.js`（`@napi-rs/canvas` 提供 Node 端 Canvas 实现；按 `wx.getImageInfo` 返回的 EXIF 方向矫正旋转后绘制，导出 JPEG quality 95（@napi-rs/canvas 质量刻度为 0-100，误用 0-1 刻度会把图压糊）；sizeType 限定 `['original']` 原图取片，拍摄/相册同策略）。字体与品牌 logo 在 `server/assets/`（汉仪旗黑 X2-65J 正文 / PT Mono 防伪码码值等宽 / 思源黑体 Bold「防伪」前缀 / brand-logo.png），进程内 `GlobalFonts.registerFromPath` 注册一次；防伪码为「前缀+码值」分字体两次绘制；行右缘固定不动，字号按基准字号量宽后整体等比缩放（浮点字号不取整，纵横比不变），使「防」左缘刚好超过上方 logo 中「相」字左缘（logoXiangL 按 alpha 通道实测 5/502）；码值字号为前缀 1.1×（略小于「防伪」二字）；两字体按 `actualBoundingBox*` 实测可见字形中心残差校正前缀基线（码值下探恒取 0——字符集仅大写+数字无下伸笔画，且 @napi-rs/canvas 的 descent 为字体级值非字形实测），**垂直中线**任意分辨率恒平（非底线对齐——思源黑 CJK 高于 PTMono 大写，底线对齐会让「防伪」中心偏高；故「防伪」底部略低于码值属预期）；「防伪」与码值各自正下方垫一个扁平椭圆软影（径向渐变，无文字投影，logo 阴影参数独立）。
- **依赖注意**：`@napi-rs/canvas` 为原生预编译二进制依赖（linux x64 glibc/musl 均有预编译包，安装即用，**无需编译**）；Docker 构建需保证 `npm ci` 能访问 npm  registry 下载平台二进制。
- **水印字段**（弹层可编辑 6 项）：施工内容 / 拍摄时间 / 天气 / 地点 / 经度 / 纬度；经纬度只填数字时前端自动补 `°E`/`°N`。服务端 `sanitizeWm` 按库列宽截断。
- **防伪码**：14 位大写字母+数字（去易混淆字符），前端弹层自动生成只读展示、可「换一换」；服务端二次校验字符集，不合规重新生成。**不落库**。
- **预填规则**：本卡已有水印照片 → 带入其 Dify 回写字段值，**经纬度加随机偏移（半径 ≤400m，对 500m 上限留余量）、拍摄时间保留日期但时分随机化为 10:00-12:00 内且与历史值不同**，避免与已有照片一致；无历史照片 → 施工内容留空、拍摄时间取当前（`YYYY.MM.DD HH:mm`）、`wx.getLocation`（gcj02）取经纬度并调 `GET /worklog/geo` 预填地点/天气，授权被拒或接口失败均留空手填。
- **腾讯位置服务对接**（`server/src/worklog/geo.js`）：逆地址解析 `https://apis.map.qq.com/ws/geocoder/v1/`（地点拼「区县·地标」，如 `汾阳市·景和府`：区县取 `address_component.district`，地标依次回退 `landmark_l2.title` → `landmark_l1.title` → `town.title` → `street`）、实时天气 `https://apis.map.qq.com/ws/weather/v1/?type=now`（拼「天气·温度」，如 `多云·23℃`）；**两接口 `location` 参数均为「纬度,经度」顺序**，`key` 走 query 参数；env `TENCENT_MAP_KEY`（腾讯位置服务控制台创建应用，key 类型须勾选 **WebServiceAPI**；未配置时返回空串，前端留空手填）。坐标系 gcj02，与 `wx.getLocation` 一致。
- **小程序权限**：`app.json` 已声明 `permission.scope.userLocation` 与 `requiredPrivateInfos: ["getLocation"]`。
- **体积限制**：worklog 路由单独 `express.json({ limit: '20mb' })`（挂在全局 12mb 之前，已解析的 body 后续解析器自动跳过）；原图限制 15MB（base64 后约 20MB）；带水印产物统一 JPEG。

## 八、已接入应用：安全日活动记录（SafeDayLogs）

app_key `safe-day`；**网页端应用**（仓库内 `SafeDayLogs/`，独立 Express 服务，与主平台分开部署），生产经 `https://j1net.com/sdl/` 访问。小程序端仅为引导页（分包 `pkg-safeday`）：展示/复制访问地址、引导电脑端使用，**不做 web-view、不在小程序内打开**；地址常量在 `miniprogram/config.js` 的 `SAFEDAY_WEB_URL`。

### 8.1 登录验证（仅授权用户可用）

- SafeDayLogs 自身不存账号：登录页提交账号密码 → 后端转发主平台 `POST /api/v1/auth/app-login`（账号密码 + `safe-day` 应用权限一次校验）→ 通过后签发自有会话 JWT 写 HttpOnly Cookie（`sdl_session`）。
- 门控：`GET /`、`GET /index.html` 无会话 302 到 `${PROXY_PREFIX}/login.html`（含前缀绝对路径，原因见第九节「网页应用二级路径部署」）；其余 `/api/*` 无会话 401；`/api/login`、`/api/logout`、`/api/callback`（Dify 回调，自带 CALLBACK_TOKEN）豁免。
- 错误映射：主平台 40111 → 401「账号或密码错误」；40301 → 403「暂无应用权限，请联系管理员开通」；主平台不可达 → 502。
- 会话：`SESSION_SECRET` 签名（未配置则随机兜底、重启全部失效），有效期 `SESSION_EXPIRES`（默认 `7d`）；权限被收回不影响已签发会话，最长存活一个会话周期。
- 生成记录不做按用户隔离（班组共享），与接入登录前行为一致。

### 8.2 SafeDayLogs 环境变量（详见 `SafeDayLogs/.env.example`）

| 变量名 | 说明 |
|--------|------|
| `J1TOOLKIT_API_URL` | 主平台 API 根地址，登录校验必需。推荐填主平台直连地址（与反代 upstream 一致，如 `http://10.2.24.13:8005`），避免绕公网域名的 hairpin 问题；两容器同一用户自定义 Docker 网络时也可用容器名 + 容器内部端口（如 `http://j1toolkit:3000`，注意不是宿主发布端口）；公网地址 `https://j1net.com/j1toolkit` 仅在服务器访问自身域名可达时可用 |
| `SESSION_SECRET` / `SESSION_EXPIRES` | 会话签名密钥 / 有效期（默认 `7d`） |
| `PROXY_PREFIX` | 反代前缀（如 `/sdl`；以二级路径形式部署时必填——即使反代剥前缀，登录跳转 Location 也用它拼接；独立域名直连留空） |
| `COOKIE_SECURE` | 生产 HTTPS 置 `true`（会话 cookie 加 Secure） |
| `DIFY_BASE_URL` / `DIFY_API_KEY` / `DIFY_USER` / `PORT` / `CALLBACK_TOKEN` | 原有配置 |

### 8.3 部署对接注意

- 主平台启动时 `db.js` 种子自动写入 `safe-day` 应用记录（sort 3）并授予 `ADMIN_USERNAME`；其他用户由管理员在「权限管理」勾选。
- SafeDayLogs 前端 fetch 与重定向一律用相对路径（`api/...`、`login.html`），配合 `PROXY_PREFIX` 剥前缀中间件，兼容反代剥/不剥前缀两种转发（与主平台同口径）；后端登录门控跳转为含前缀绝对路径、裸前缀（`/sdl` 无尾斜杠）302 补尾斜杠，见第九节「网页应用二级路径部署」。
- 小程序侧无需配置新域名（引导页不发起任何网络请求）。

## 九、既有约定（踩坑记录，务必遵守）

### UI / 交互

- **设计规范**：方案五 token（见 `AGENTS.md` 第四节），全局 `--td-*` 变量已覆盖；卡片/发丝线/警示斜纹/描边徽章直接用 `app.wxss` 通用类；
- **右上角禁放按钮**：微信胶囊区域不可点，`navbar` 组件已移除右侧插槽，操作类按钮放页面底部（参照会话页 `create-bar`）；
- **状态栏颜色**：深色页（如登录页）自行 `wx.setNavigationBarColor` 设白色，其余页面由 `navbar` 组件自动重置为黑色；
- **键盘处理（输入栏三件套，参照 `chat` 页）**：① `textarea` 用 `adjust-position="{{false}}"` + `bindkeyboardheightchange`，把输入栏 `margin-bottom` 动态设为键盘高度——整页不被顶起、标题栏原位保留；② 输入栏底边与键盘顶**齐平**即可，不要额外加缝隙；③ 底 padding 要动态：贴屏时含 `env(safe-area-inset-bottom)`，键盘弹起后降为普通内边距；
- **按压反馈**：可点击的自定义 view 加 `hover-class`（全局类 `hover-scale` / `hover-bg-warm` / `hover-opacity`）；入场动画用全局 `fadeUp` / `fadeIn`，克制在 150~300ms；
- **图标图片**：图标用 `t-icon` 名称；图片素材放 `miniprogram/assets/`（ASCII 文件名）；分享图放 `miniprogram/images/share/`（生成方式见第五章第 5 步）；
- **tdesign 组件坑**：t-check-tag 的 `content` 属性缺省会渲染出 "null" 文本，需显式传 `content=""`；chips 类容器用 `<view>` 不要用 `<text>`（text 不参与 flex 布局）；t-calendar 无单元格插槽，定制用 `format` 属性——它是函数型属性，**setData 与 wxml 绑定传函数在微信下都会被剥离**，可靠做法：format 定义为模块级函数，`selectComponent` 后直写 `cal.base.format = fn`（纯 JS 引用无序列化），再手动 `cal.calcMonths()` 重算；**`switch-mode="month"` 时网格渲染的是 `currentMonth`（由 months 推导），还须补调 `cal.updateCurrentMonth()`**，否则只重算 months 不上屏、点过日期才着色。

### 工程

- **依赖与构建**：装依赖后必须重新「工具 → 构建 npm」；若构建报 `Unexpected token`（某依赖用了新语法），在对应 `package.json` 加 `overrides` 降级该依赖（参照 argparse 1.0.10 案例）；
- **请求封装**：页面一律用 `utils/request.js` 的 `request()`（自动带 token、401 清理并跳登录；自检类调用传 `authRedirect: false`）；
- **自动登录**：不要绕过 `app.js` 的 bootstrap 门控；新入口页参照首页的 `gate` 模式；
- **用户信息**：`getApp().applyUser(user)` 统一写入缓存与"可静默微信登录"标记。

### 部署 / 运维

- 后端：`npm run start` 启动，`/healthz` 探活；首次启动自动建表/补列/写种子；
- 反代：`PROXY_PREFIX` 已兼容剥/不剥前缀；SSE 必须 `proxy_buffering off` + `proxy_read_timeout 300s`；图片上传需 `client_max_body_size 20m`；
- **网页应用二级路径部署（`/wl`、`/sdl` 这类前缀）三件套**：① 后端所有浏览器跳转（登录门控等）一律用**含前缀的绝对路径**（`${PROXY_PREFIX}/login.html`），不用相对 Location——相对路径在裸前缀 URL（`域名/wl`，无尾斜杠）下会被浏览器按域根解析，跳成 `域名/login.html` 丢前缀，且改反代 `$host` 与此无关、解决不了；② 裸前缀请求先 302 补尾斜杠（`/wl` → `/wl/`）——否则已加载页面内的相对路径（`api/...`）同样按域根解析；③ 前端 fetch/跳转一律用相对路径（不带前导 `/`），配合后端 `PROXY_PREFIX` 剥前缀中间件，兼容反代剥/不剥前缀两种转发。**只要以二级路径形式部署，`PROXY_PREFIX` 必填**（即使反代剥前缀：拼跳转 Location 要用它；读取时去末尾斜杠）；独立域名直连才留空。参照实现：`WorkLogs/server.js`、`SafeDayLogs/server.js`；
- 小程序后台域名：request 合法域名 `https://j1net.com`；出工日志 COS 域名需配置 downloadFile 合法域名；
- 环境变量全部走 env，`.env.example` 同步更新，真实 `.env` 不入仓。

## 十、新应用验收清单

- [ ] `sys_app` 记录已写入（`status=1`），权限管理页可授权
- [ ] `app.json` 分包已注册，宫格点击能进入
- [ ] 页面风格与本体一致（token/卡片/按压反馈/状态栏黑色/右上角无按钮）
- [ ] 页面已加 `onShareAppMessage`（走 `utils/share.js`），分享图已生成并登记 `SHARE_IMAGES`
- [ ] 接口挂 `auth` + `requireApp`，env 与 `.env.example` 同步
- [ ] `node --check` / JSON 校验通过，构建 npm 通过，上传无「主包未使用文件」告警
- [ ] 真机验证：登录态、键盘、长列表、图片上传（如涉及）
