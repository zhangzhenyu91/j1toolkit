# 开发指南 —— 添加新应用

> 面向后续迭代的操作手册。规则类内容以 `AGENTS.md` 为准，需求与方案以 `项目文档.md` 为准，本文是两者的落地执行版。

## 一、现状速览

```
微信小程序 ──HTTPS──▶ https://j1net.com/j1toolkit/（1Panel 反代，剥前缀兼容）
                          │
                          ▼
                  server/（Express，npm run start，0.0.0.0:PORT）
                    ├─ MySQL：sys_user / sys_app / sys_user_app
                    ├─ Redis：JWT 黑名单
                    └─ WeKnora：Call Me 对话代理（SSE）
```

已实现：登录（账号密码 + 微信 + 自动登录续期）、首页权限宫格、「我的」（含管理员功能区）、员工管理、权限管理、Call Me（AI 知识库对话，分包 `pkg-callme`）。

关键目录：

| 目录 | 内容 |
|------|------|
| `miniprogram/` | 主包（登录/首页/我的/管理页 + 公共组件 navbar、tabbar + utils） |
| `miniprogram/pkg-callme/` | Call Me 分包（新应用的参照模板） |
| `server/src/` | 后端：routes / middleware / services / db.js（建表+种子） |

## 二、添加新应用（四步 checklist）

### 1. 数据库登记应用（必须）

向 `sys_app` 插入记录：

```sql
INSERT INTO sys_app (app_key, name, icon, path, sort, status)
VALUES ('my-app', '应用名称', 'robot', '/pkg-myapp/pages/index/index', 2, 1);
```

- `icon` 填 TDesign 图标名（不带 `t-icon-` 前缀，如 `robot`/`book`/`service`）；
- `path` 填分包内页面路径（第 2 步创建）；
- 也可以仿照 Call Me，在 `server/src/db.js` 的 `APP_CALL_ME` 旁加一条种子，启动时自动写入。

给用户授权：**管理员在「我的 → 权限管理」勾选即可**（推荐），无需手写 SQL。

### 2. 小程序分包（必须）

1. 参照 `pkg-callme/` 新建 `pkg-myapp/pages/...`；
2. `app.json` 的 `subPackages` 注册：`{ "root": "pkg-myapp", "name": "myapp", "pages": [...] }`；
3. 首页宫格由 `/app/list` 驱动，授权后自动出现入口，**无需改首页代码**；
4. 分包若有自己的 npm 依赖：
   - 在 `pkg-myapp/` 下新建 `package.json` 并 `npm install`（规矩：主包用 → `miniprogram/` 装；分包专用 → 分包目录装）；
   - `project.config.json` 的 `packNpmRelationList` 加一行 `{ "packageJsonPath": "./pkg-myapp/package.json", "miniprogramNpmDistDir": "./pkg-myapp" }`；
   - 重新「构建 npm」。

### 3. 后端接口（按需）

1. 新建 `server/src/routes/myapp.js`，路由统一挂中间件：`router.use(auth, requireApp('my-app'))`（登录 + 应用权限双校验）；
2. `server/src/index.js` 挂载：`app.use('/api/v1/myapp', require('./routes/myapp'))`；
3. 对接外部服务（Dify 等）：配置走 env——`config.js` 增加读取项、`.env.example` 增加示例、service 里加 `ensureConfigured()` 守卫（参照 `services/weknora.js`）；
4. SSE 流式场景直接参照 `routes/callme.js`：`X-Accel-Buffering: no` 响应头、15s 心跳、`utils/sse.js` 解析器、客户端断开销毁上游。

### 4. 自检与交付

`node --check` 全部 JS、JSON 合法性校验；本地无法模拟的项在交付说明中如实标注「待云端/真机验证」。

## 三、既有约定（踩坑记录，务必遵守）

### UI / 交互

- **设计规范**：方案五 token（见 `AGENTS.md` 第四节 / `项目文档.md` 第八章），全局 `--td-*` 变量已覆盖；卡片/发丝线/警示斜纹/描边徽章直接用 `app.wxss` 通用类；
- **右上角禁放按钮**：微信胶囊区域不可点，`navbar` 组件已移除右侧插槽，操作类按钮放页面底部（参照会话页 `create-bar`）；
- **状态栏颜色**：深色页（如登录页）自行 `wx.setNavigationBarColor` 设白色，其余页面由 `navbar` 组件自动重置为黑色；
- **键盘处理（输入栏三件套，参照 `chat` 页）**：① `textarea` 用 `adjust-position="{{false}}"` + `bindkeyboardheightchange`，把输入栏 `margin-bottom` 动态设为键盘高度——整页不被顶起、标题栏原位保留；② 输入栏底边与键盘顶**齐平**即可，不要额外加缝隙（会露出一条异色细缝，反而难看），栏内自身的白色内边距就是留白；③ 底 padding 要动态：贴屏时含 `env(safe-area-inset-bottom)` 躲刘海屏手势条，键盘弹起浮空后降为普通内边距（否则键盘上方多出一条白色空带）；
- **按压反馈**：可点击的自定义 view 加 `hover-class`（全局类 `hover-scale` / `hover-bg-warm` / `hover-opacity`）；入场动画用全局 `fadeUp` / `fadeIn`，克制在 150~300ms；
- **图标图片**：图标用 `t-icon` 名称；图片素材放 `miniprogram/assets/`（ASCII 文件名）。

### 工程

- **依赖与构建**：装依赖后必须重新「工具 → 构建 npm」；若构建报 `Unexpected token`（某依赖用了新语法），在对应 `package.json` 加 `overrides` 降级该依赖（参照 argparse 1.0.10 案例）；
- **请求封装**：页面一律用 `utils/request.js` 的 `request()`（自动带 token、401 清理并跳登录；自检类调用传 `authRedirect: false`）；
- **自动登录**：不要绕过 `app.js` 的 bootstrap 门控；新入口页参照首页的 `gate` 模式；
- **用户信息**：`getApp().applyUser(user)` 统一写入缓存与"可静默微信登录"标记。

### 部署 / 运维

- 后端：`npm run start` 启动，`/healthz` 探活；首次启动自动建表/补列/写种子；
- 反代：`PROXY_PREFIX` 已兼容剥/不剥前缀；SSE 必须 `proxy_buffering off` + `proxy_read_timeout 300s`；图片上传需 `client_max_body_size 20m`；
- 小程序后台域名：request 合法域名 `https://j1net.com`；新增外部资源域名时按需配置 downloadFile；
- 环境变量全部走 env，`.env.example` 同步更新，真实 `.env` 不入仓。

## 四、新应用验收清单

- [ ] `sys_app` 记录已写入（`status=1`），权限管理页可授权
- [ ] `app.json` 分包已注册，宫格点击能进入
- [ ] 页面风格与本体一致（token/卡片/按压反馈/状态栏黑色/右上角无按钮）
- [ ] 接口挂 `auth` + `requireApp`，env 与 `.env.example` 同步
- [ ] `node --check` / JSON 校验通过，构建 npm 通过，上传无「主包未使用文件」告警
- [ ] 真机验证：登录态、键盘、长列表、图片上传（如涉及）
